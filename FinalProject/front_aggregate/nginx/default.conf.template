server {
    listen          80;
    server_name     web;
    location / {
        root   /usr/share/nginx/html;
    }
    location /api/1/ {
        proxy_pass  http://${BACKEND_URL}:${BACKEND_PORT}/;
    }
}
